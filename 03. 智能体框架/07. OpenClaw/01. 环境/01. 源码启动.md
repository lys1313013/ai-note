
pnpm ui:build 
pnpm build


pnpm dev dashboard --no-open

token在 openclaw.json 中获取


防止桌面应用干扰
export OPENCLAW_LAUNCHD_LABEL=none


开发
```
# 第一次初始化
pnpm dev configure

# 启动 ui
pnpm ui:dev
# 启动后端
pnpm dev gateway run
```






```bash
# 1. 停止并卸载 launchd 服务
launchctl stop ai.openclaw.gateway
launchctl unload ~/Library/LaunchAgents/ai.openclaw.gateway.plist

# 2. 删除 plist 文件
rm ~/Library/LaunchAgents/ai.openclaw.gateway.plist

# 3. 删除全局 npm 包（如果有的话）
npm uninstall -g openclaw

# 4. 删除软链
rm /opt/homebrew/bin/openclaw
rm /opt/homebrew/lib/node_modules/openclaw
```