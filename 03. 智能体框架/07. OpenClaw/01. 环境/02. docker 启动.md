# docker 启动



查看待审批请求 
 docker compose exec openclaw-gateway node dist/index.js devices list

审批请求 （将 <requestId> 替换为上面列表中的 ID）：
docker compose exec openclaw-gateway node dist/index.js devices approve <requestId>